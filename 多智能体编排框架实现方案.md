# 面向公司内部的多智能体编排框架（MVP）实现方案

> 文档状态：P1-P4 已实现，两个业务原型与 30 条黄金用例评测器已落地，量化数据待验收回填
> 定位：知行股份实习期间的增量项目，借鉴 Shannon 开源框架的架构思想，面向公司内部落地一套可复用的多智能体编排框架，支撑人事、风控、财务、招采等业务部门的 Agent 场景。

## 1. 项目背景

公司各业务部门（人事、风控、财务、招采）正在探索 AI Agent 的应用落地，但存在以下问题：

- 缺乏统一的 Agent 编排与调度能力，各场景各自实现，能力无法复用
- 复杂任务缺少任务拆解、依赖执行与故障恢复机制
- LLM 调用成本不可控，缺少预算限制与模型降级策略
- 多智能体协作时缺少共享上下文与状态同步机制

实习期间的目标：基于对 Shannon 等多智能体编排框架的架构研究，结合公司实际业务场景，设计并实现一套轻量级、可复用的内部多智能体编排框架，并完成 2-3 个业务 Agent 原型验证。

## 2. 目标与边界

### 目标

- 实现统一的 Agent 任务接入入口（REST/gRPC API，飞书机器人可选）
- 实现任务拆解、复杂度评分、策略选择与调度
- 支持 Simple（单 Agent 直接响应）、DAG（多步骤依赖任务）、React（工具循环）等执行策略，Swarm 作为后续扩展方向
- 基于 Temporal（或轻量级替代方案）实现任务持久化、重试与故障恢复
- 实现智能体间共享上下文的工作区（Workspace）机制
- 实现 Token 预算控制与模型自动降级，降低 LLM 调用成本
- 沉淀人事制度问答、风控条款审查 2 个业务 Agent 原型与内部评测结果

### 非目标

- 不研发底层 LLM、向量模型或检索算法，优先复用现有 RAG/Milvus/ES 能力
- 不重复实现 Shannon 的全部功能，只裁剪并落地与公司场景匹配的核心能力
- 不承诺一期完成多租户、权限审计等企业级安全能力，按迭代推进

## 3. 总体架构

借鉴 Shannon 的分层设计思想：

```text
接入层: REST/gRPC API | 飞书机器人（可选）
编排层: Orchestrator Router
        任务拆解 | 复杂度评分 | 策略选择 | 预算中间件
策略层: Simple / DAG / React / Swarm（二期）
        组合模式: Parallel / Sequential / Hybrid + Reflection
执行层: Activities
        LLM 调用 | RAG 检索 | 工具调用 | 飞书 API | 数据库
基础设施: Temporal | Redis | PostgreSQL | 事件流与监控
```

核心设计原则：Workflow 只做确定性编排（规划、路由、分支、等待），所有外部副作用（LLM 调用、Redis、数据库、事件流）封装在 Activity 中。这样任务具备持久化、重试和故障恢复能力。

## 4. 核心设计决策

### 4.1 为什么借鉴 Shannon 而不是从零实现

Shannon 的架构将「Router 路由 → Strategy Workflow → Pattern 模式 → Activity 执行」分层，具备以下可直接借鉴的设计：

- 工作流引擎驱动的可靠性编排
- 基于复杂度评分的动态策略路由
- 多策略工作流与可组合模式（Parallel/Sequential/Hybrid + Reflection 等）
- Swarm 模式下 Lead Agent 与角色 Agent 的事件驱动协作
- 会话级 Workspace 共享上下文
- Token 预算控制与模型降级

自研框架只需实现与公司场景匹配的核心子集，降低落地风险。

### 4.2 为什么选择 Temporal 作为工作流引擎

- 持久化 Workflow 历史，任务中断后可恢复
- 内置重试、超时、信号与查询机制
- 时间旅行调试，便于排查长链路问题
- 相比自研队列状态机，可靠性更高、实现成本更低

若公司基础设施受限，可用轻量替代方案：Redis Streams + 自研状态机，但需要自行实现重试与恢复。

### 4.3 动态策略路由

Router 对任务进行拆解与复杂度评分：

- 低复杂度任务（评分 < 0.3）直接走 Simple 策略
- 复杂任务根据认知策略路由到 DAG / React，Swarm 作为扩展方向
- 可扩展基于历史成功率的 epsilon-greedy 学习路由（10% 探索，90% 利用）

### 4.4 Workspace 共享上下文

每个会话拥有独立 Workspace，用于存放中间结果、文件与上下文摘要：

- 文件系统隔离或 Redis 存储，支持多 Agent 读写
- Swarm 模式下 Agent 通过 Workspace 发布与读取共享结果
- 避免上下文无限膨胀，配合 Token 预算控制

### 4.5 Token 预算控制

- 每个任务 / 每个 Agent 设置独立 Token 上限
- 超限时自动降级模型（如从高配模型降级到低配模型）或提前终止
- 记录 token_usage 明细，支持成本统计与优化

### 4.6 可观测性

- SSE 事件流实时输出任务状态
- 任务时间线记录拆解、路由、执行、重试全过程
- 预留 Prometheus / OpenTelemetry 指标接入点

## 5. 功能模块设计

| 模块 | 职责 | 关键技术点 |
| --- | --- | --- |
| Coordinator | 任务拆解、策略选择、调度 | 复杂度评分、路由策略、预算中间件 |
| Simple 策略 | 单 Agent 直接响应 | 一次 LLM 调用 |
| DAG 策略 | 多步骤依赖任务 | 依赖图、拓扑排序、Parallel/Sequential/Hybrid |
| React 策略 | 工具循环推理 | Reason-Act-Observe、工具调用 |
| Swarm 策略（二期） | 多智能体协作 | Lead Agent + 角色 Agent + Workspace + P2P |
| Temporal 接入 | 任务持久化与恢复 | Workflow / Activity 注册、队列、信号 |
| Workspace | 共享上下文与中间结果 | 文件系统或 Redis、会话隔离 |
| Token 预算 | 成本控制 | 限额、降级、token_usage 记录 |
| 工具接入 | 外部能力集成 | RAG、飞书 OpenAPI、数据库、HTTP API |
| 事件流 | 可观测性 | SSE、任务时间线 |

## 6. 技术栈

- 编程语言：Python（复用 AgentChat 的 FastAPI/LangChain 经验）
- 工作流引擎：Temporal（或轻量级自研方案）
- LLM：OpenAI API / 开源模型，支持模型降级
- 检索：Milvus / Elasticsearch / ChromaDB，复用现有 RAG 链路
- 存储：Redis（缓存、中间结果）、PostgreSQL / MySQL（任务与 Token 记录）
- 协议：REST / gRPC
- 可选接入：飞书开放平台（复用已有飞书项目经验）

## 7. 实现步骤

| 阶段 | 时间 | 目标 | 交付物 |
| --- | --- | --- | --- |
| 调研设计 | 第 1 周 | 梳理 Shannon 的 Router/Workflow/Activity/Patterns，定义 TaskInput、API 与策略接口 | 架构图、接口设计、业务场景清单 |
| 最小闭环 | 第 2-3 周 | 跑通「提问 → 路由 → Simple/DAG → 返回」，接入 Temporal 与 Token 记录 | 可运行 MVP API + 1 个业务场景 |
| 能力增强 | 第 4-5 周 | 增加 React 工具调用、RAG、Workspace、预算降级、SSE 事件流 | 框架能力齐全，覆盖 2 个场景 |
| 协作与原型 | 第 6-7 周 | Swarm 的 Lead/Agent 协作（可选），搭建 2-3 个业务 Agent 原型 | 可现场演示的原型 |
| 验证沉淀 | 第 8 周 | 4 个部门场景适配，内部评测，输出文档与部署脚本 | 评测报告、复用指南、演示 demo |

### 各阶段验收标准

1. 调研设计：输出架构图与接口设计，业务场景清单包含每个部门的高频问题
2. 最小闭环：Simple 与 DAG 场景可在本地运行，任务中断后能恢复或正确重试
3. 能力增强：React 场景能调用 RAG 工具并返回结果，Token 超限可自动降级
4. 协作与原型：2-3 个原型可通过统一 API 调用，演示过程可复现
5. 验证沉淀：量化口径明确，文档覆盖部署、接入与扩展方式

## 8. 预期成果

### 框架能力

- 统一任务接入与动态路由
- Simple / DAG / React 多策略执行，Swarm 作为可扩展方向
- 任务持久化、重试与故障恢复
- Workspace 共享上下文
- Token 预算与模型降级
- SSE 事件流与任务时间线

### 业务原型

从人事、风控、财务、招采中落地 2-3 个可演示场景，例如：

- 人事制度问答 Agent
- 风控条款审查 Agent
- 财务报销咨询 Agent
- 招采合同问答 Agent

### 量化口径（内部评测）

- 任务成功率：定义任务完成标准，说明基线来源与样本量
- 平均单任务 LLM 成本：对比无预算控制 / 有预算控制的 Token 消耗
- 故障恢复时长：任务中断后恢复或重试的耗时
- 并发任务支撑数：压测口径需说明硬件与模型限制

### 文档沉淀

- 架构设计文档
- 技术选型说明（为什么借鉴 Shannon、为什么用 Temporal）
- 接口规范
- 部署脚本与二次开发指南

## 9. 风险与说明

- 公司基础设施依赖：Temporal/Redis/PostgreSQL 可能需要部署或申请资源，若受限则使用轻量替代方案
- 量化指标的可解释性：所有数字需基于内部评测，避免不可验证的估算
- 与已有实习工作的衔接：燃料温控 AI 问答模块提供 LLM 工具调用与降级经验，AgentChat 提供 RAG/MCP/Skill 能力，本框架复用并抽象为统一编排能力
- 飞书集成可选：仅作为接入验证通道，不作为核心卖点

## 10. 与简历主线的关系

该项目的简历叙事建议按「调研选型 → 架构设计 → 实现落地 → 验证沉淀」组织：

1. 调研对比 Shannon 等框架，明确公司缺少统一编排能力，完成技术选型
2. 借鉴 Shannon 分层架构，设计并实现内部多智能体编排框架，支持动态路由与多策略执行
3. 基于 Temporal 实现任务持久化与故障恢复，结合 Workspace 与 Token 预算提升可靠性与成本可控性
4. 面向 4 个部门完成场景适配与 2-3 个 Agent 原型，沉淀可复用能力

## 11. 实习经历简历条目（待验收后更新量化数据）

> 公司名沿用现有简历中的“知行股份”，如需按营业执照改为“知行良知”可全局替换。
> 所有 `[预期：...]` 为占位值，项目验收后根据实际评测结果替换。

**知行股份 - 信息中心 - AI Agent实习生 | 成都 | 2026.07 - 至今**

- 调研 Shannon 等开源多智能体编排框架，结合人事/风控/财务/招采 4 个部门需求梳理，明确公司缺乏统一 Agent 编排与调度能力，完成 Python + Temporal + Redis/PostgreSQL 技术选型，输出整体架构设计与接口规范。
- 借鉴 Shannon 分层架构设计并实现内部多智能体编排框架：Router 基于复杂度评分实现动态策略路由，支持 Simple/DAG/React/Swarm 多策略执行；基于 Temporal 将外部副作用封装为 Activity，实现任务持久化、自动重试与故障恢复，[预期：任务成功率≥90%、故障恢复≤10s]。
- 设计会话级 Workspace 共享上下文与 Token 预算控制：多智能体通过共享工作区同步中间结果，按任务/Agent 设置 Token 上限并自动降级模型，[预期：单任务 LLM 成本降低 30% 以上、上下文稳定≤4k tokens]。
- 面向 4 个部门完成场景适配并搭建 [2-3] 个 Agent 原型（如人事制度问答、风控条款审查、财务报销咨询），基于 SSE 事件流与任务时间线实现全链路可观测，[预期：支撑并发任务≥20，沉淀可复用场景与二次开发指南]。

### 使用说明

1. 第 1 条对应“调研选型”，第 2 条对应“架构设计与实现”，第 3 条对应“协作与成本控制”，第 4 条对应“场景落地与验证”。
2. 与现有“燃料温控 AI 问答模块”条目合并为同一段实习经历时，可将本段 4 条追加在温控模块之后，或按简历版面压缩为 2-3 条。
3. 量化占位值替换前，先固化评测口径：成功率定义、样本量、压测硬件与模型配置。
